syntax = "proto3";

package rest_communication;

service RestCommunication {
  rpc SendRest (RestRequest) returns (RestResponse) {}
}

message RestRequest {
  string mission_status = 1;
}

message RestResponse {
  string confirmation = 1;
}